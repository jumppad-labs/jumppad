# Terraform version

The `terraform version` command displays the current version of Terraform and all installed plugins.

<Task id="installation/terraform_version">
  <Instructions>
    Verify that the `terraform` binary is the correct version by running `terraform version`.
  </Instructions>
  <Conditions>
    <Condition 
      id="version_command"
      target="vscode"
      description="Use the terraform version command" 
    />
  </Conditions>
</Task>